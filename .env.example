# ============================================================
# CEJ Cotizador Platform
# Environment Variables Template
# Copy this file to .env.local and fill in the values.
# All variables are validated by config/env.ts (Zod schema).
# ============================================================


# ------------------------------------------------------------
# Meta Tracking (required for lead attribution)
# ------------------------------------------------------------

# Your Meta (Facebook) Pixel ID — browser-side tracking.
NEXT_PUBLIC_PIXEL_ID=XXXXXXXXXXXXXXX

# Meta System User Access Token — used for server-side CAPI events.
# Keep this SECRET. Never prefix with NEXT_PUBLIC_.
# Generate at: business.facebook.com → System Users
FB_ACCESS_TOKEN=your-meta-system-user-token


# ------------------------------------------------------------
# Google Analytics
# ------------------------------------------------------------

# GA4 Measurement ID (format: G-XXXXXXXXXX)
NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX


# ------------------------------------------------------------
# Contact Information
# ------------------------------------------------------------

# WhatsApp number in E.164 format (no + or spaces).
# Example: 5216561234567  =>  +52 (1) 656 123 4567
NEXT_PUBLIC_WHATSAPP_NUMBER=521656XXXXXXX

# Main phone number for optional "Call" CTA.
# Leave blank to disable the phone CTA.
NEXT_PUBLIC_PHONE=656XXXXXXX


# ------------------------------------------------------------
# Site Configuration
# ------------------------------------------------------------

# Full base URL of the deployed site (no trailing slash).
# Used for canonical links, OG tags, and SEO.
NEXT_PUBLIC_SITE_URL=https://concretodejuarez.com

# Local development base URL. Used by some server-side utilities
# to generate absolute URLs during local development.
NEXT_LOCAL_SITE_URL=http://localhost:3000

# Company or brand name — used in metadata and SEO schemas.
NEXT_PUBLIC_BRAND_NAME=Concreto y Equipos de Juárez

# Default currency for price events and display.
NEXT_PUBLIC_CURRENCY=MXN


# ------------------------------------------------------------
# Supabase Configuration
# App runs in Fail-Open mode if these are absent:
# orders won't persist but the UX (WhatsApp handoff) still works.
# ------------------------------------------------------------

# Supabase project URL (public — safe to expose).
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Supabase anon/public key — used by the browser client.
# This is safe to expose publicly.
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# Supabase service role key — used ONLY in Server Actions.
# Keep this SECRET. Never prefix with NEXT_PUBLIC_.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-secret


# ------------------------------------------------------------
# Monitoring & Observability (optional)
# ------------------------------------------------------------

# Webhook URL for system error alerts (e.g., Slack incoming webhook).
# If absent, errors are logged to the console only.
MONITORING_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL


# ------------------------------------------------------------
# Testing Support (dev/CI only)
# ------------------------------------------------------------

# Set to "true" to enable mock modes for Playwright E2E tests.
# Never set this to "true" in production.
ENABLE_E2E_MOCKS=false
